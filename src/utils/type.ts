export type BoardObject = {
  boardSize: number;
};

export type RobotPositionObject = {
  x: number;
  y: number;
  direction: string;
};

export type RobotMovementsObject = {
  move: string;
  isMove: boolean;
};
